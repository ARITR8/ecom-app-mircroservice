spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  application:
    name: order-service
  config:
    import: "optional:configserver:http://localhost:8887"
